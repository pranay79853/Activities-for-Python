# 1) Initialize the variable `var` with the value 10.
var = 10
# 2) Use a `while` loop that runs as long as `var` is greater than 0.
while var > 0:
    var -= 1
    # 4) Check if the value of `var` is equal to 5.
    if var == 5:
        # 5) If `var` equals 5, use the `continue` statement.
        continue
    # 6) Print the current value of `var`.
    print(var)
# 3) Decrease the value of `var` by 1 during each loop iteration.
print("Goodbye!")
# 4) Check if the value of `var` is equal to 5.

# 5) If `var` equals 5, use the `continue` statement.

# (This skips the remaining code inside the loop for that iteration.)

# 6) Print the current value of `var`.

# (The value 5 will not be printed because of `continue`.)

# 7) After the loop finishes, print "Good bye!".